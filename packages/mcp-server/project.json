{
  "name": "@nicholasosto/mcp-server",
  "sourceType": "mcp-server",
  "tags": ["scope:node", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.build.json",
        "cwd": "packages/mcp-server"
      },
      "cache": true,
      "inputs": ["default"],
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/mcp-server"
      },
      "cache": true,
      "inputs": ["default"],
      "outputs": []
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "packages/mcp-server"
      }
    }
  }
}
