{
  "name": "@nicholasosto/name-generator",
  "sourceType": "name-generator",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rbxtsc",
        "cwd": "packages/name-generator"
      },
      "cache": true,
      "inputs": ["default"],
      "outputs": ["{projectRoot}/out"]
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rbxtsc -w",
        "cwd": "packages/name-generator"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf out include",
        "cwd": "packages/name-generator"
      }
    }
  }
}
